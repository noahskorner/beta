{
  "name": "beta",
  "version": "1.0.0",
  "main": "index.js",
  "packageManager": "npm@10.7.0",
  "scripts": {
    "clean": "find . ( -name node_modules -o -name .turbo -o -name .next ) -type d -prune -exec rm -rf '{}' +",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "postinstall": "npm run prisma:generate",
    "prisma:migrate:dev": "turbo run prisma:migrate:dev",
    "prisma:generate": "turbo run prisma:generate",
    "setup": "docker compose up -d && npm run prisma:migrate:dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "dotenv-cli": "^8.0.0",
    "turbo": "^2.6.3"
  }
}
